{
  "name": "gtfs-rt-analyzer-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Restricted CORS proxy that fetches allow-listed GTFS-Realtime feeds for the browser-side analyzer.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}
