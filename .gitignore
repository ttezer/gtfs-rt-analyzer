# Rust build cache
/target/

# Fuzz artifacts
/fuzz/target/
# corpus/ ÜRETİLİR ve izlenmez: ölçüldü, tek koşumdan sonra 24 MB ve her koşumda
# değişiyor. Elle hazırlanan tohumlar `fuzz/seeds/` altında, çökme girdileri
# `fuzz/artifacts/` altında İZLENİR — ikisi de küçük ve kanıt değeri taşır.
/fuzz/corpus/

# Local-only files (not for version control)
/notgit/
/samples/
/scripts/node_modules/

# OS
.DS_Store
Thumbs.db

# Editor temp files
*.swp
*.swo
*~

# IDE configs
.vscode/
.idea/

# Environment variables
.env
.env.local
